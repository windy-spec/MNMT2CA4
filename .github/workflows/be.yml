name: Deploy Backend

on:
  # Tự động chạy khi push vào nhánh main và có sửa đổi trong thư mục backend
  push:
    branches: ["main"]
    paths: ["backend/**"]
  # Cho phép bấm nút chạy thủ công
  workflow_dispatch:

jobs:
  deploy-be:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Lấy code
        uses: actions/checkout@v4

      - name: 📂 Upload PHP vào htdocs/api
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./backend/
          server-dir: /htdocs/api/
          protocol: ftp
          port: 21
