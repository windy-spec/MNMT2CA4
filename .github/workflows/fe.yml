name: Deploy Frontend

on:
  # Tự động chạy khi push vào nhánh main và có sửa đổi trong thư mục frontend
  push:
    branches: ["main"]
    paths: ["frontend/**"]
  # Cho phép bấm nút chạy thủ công (Tab Actions)
  workflow_dispatch:

jobs:
  deploy-fe:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Lấy code
        uses: actions/checkout@v4

      - name: 📂 Upload HTML vào htdocs
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./frontend/
          server-dir: /htdocs/
          # Bảo vệ thư mục api và git
          exclude: |
            api/**
            **/.git*
          # Cấu hình giảm lỗi kết nối InfinityFree
          protocol: ftp
          port: 21
