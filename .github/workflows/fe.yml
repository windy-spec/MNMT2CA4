name: Deploy Frontend

on:
  push:
    branches: ["main"]
    paths: ["frontend/**"] # Chỉ chạy khi sửa Frontend

jobs:
  deploy-fe:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Lấy code
        uses: actions/checkout@v4

      - name: 📂 Upload HTML vào htdocs
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./frontend/
          server-dir: /htdocs/
          exclude: |
            api/**
            **/.git*
          # --- THÊM 2 DÒNG NÀY ---
          protocol: ftp
          port: 21
